import React, { Component } from "react";

class Lattice extends Component {
    render(){
        return (
            <div className="burger-component green mb-1"></div>
        );
    }
}

export default Lattice;